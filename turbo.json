{
  "$schema": "https://turborepo.com/schema.json",
  "globalEnv": ["NODE_ENV"],
  "globalDependencies": [".env", "backend/.env", "frontend/.env"],
  "tasks": {
    "setup": {
      "cache": false,
      "outputs": ["backend/.venv/**"],
      "inputs": ["backend/requirements.txt"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["frontend/.next/**", "!frontend/.next/cache/**", "backend/dist/**"],
      "env": ["NEXT_PUBLIC_API_URL", "OPENAI_API_KEY", "OPENAI_MODEL"],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env.production.local",
        ".env.local",
        ".env.production",
        ".env",
        "backend/.env*",
        "frontend/.env*"
      ]
    },
    "dev": {
      "dependsOn": ["setup"],
      "cache": false,
      "persistent": true,
      "env": ["NEXT_PUBLIC_API_URL", "OPENAI_API_KEY", "OPENAI_MODEL"],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env.development.local",
        ".env.local",
        ".env.development",
        ".env",
        "backend/.env*",
        "frontend/.env*"
      ]
    },
    "lint": {
      "dependsOn": ["^lint"],
      "outputs": []
    },
    "format": {
      "cache": false,
      "outputs": []
    },
    "format:check": {
      "cache": false,
      "outputs": []
    },
    "check": {
      "dependsOn": ["^check"],
      "cache": false,
      "outputs": []
    },
    "type-check": {
      "dependsOn": ["^type-check"],
      "outputs": []
    },
    "clean": {
      "cache": false,
      "outputs": []
    }
  }
}
