{
  "$schema": "https://turborepo.com/schema.json",
  "globalEnv": ["NODE_ENV"],
  "globalDependencies": [".env", "backend/.env", "frontend/.env"],
  "tasks": {
    "setup": {
      "cache": false,
      "outputs": ["backend/.venv/**"],
      "inputs": ["backend/requirements.txt"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["frontend/.next/**", "!frontend/.next/cache/**", "backend/dist/**"],
      "env": ["OPENAI_API_KEY"],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env.production.local",
        ".env.local",
        ".env.production",
        ".env",
        "backend/.env*",
        "frontend/.env*"
      ]
    },
    "dev": {
      "dependsOn": ["setup"],
      "cache": false,
      "persistent": true,
      "env": ["OPENAI_API_KEY"],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env.development.local",
        ".env.local",
        ".env.development",
        ".env",
        "backend/.env*",
        "frontend/.env*"
      ]
    },
    "lint": {
      "dependsOn": ["^lint"],
      "outputs": [],
      "inputs": ["**/*.py", "**/*.{js,ts,jsx,tsx,json}", "ruff.toml", "pyproject.toml", "biome.json"]
    },
    "lint:fix": {
      "dependsOn": ["^lint:fix"],
      "cache": false,
      "outputs": [],
      "inputs": ["**/*.py", "**/*.{js,ts,jsx,tsx,json}", "ruff.toml", "pyproject.toml", "biome.json"]
    },
    "format": {
      "cache": false,
      "outputs": [],
      "inputs": ["**/*.py", "**/*.{js,ts,jsx,tsx,json}", "ruff.toml", "pyproject.toml", "biome.json"]
    },
    "format:check": {
      "outputs": [],
      "inputs": ["**/*.py", "**/*.{js,ts,jsx,tsx,json}", "ruff.toml", "pyproject.toml", "biome.json"]
    },
    "check": {
      "dependsOn": ["^check"],
      "outputs": [],
      "inputs": ["**/*.py", "**/*.{js,ts,jsx,tsx,json}", "ruff.toml", "pyproject.toml", "biome.json"]
    },
    "check:fix": {
      "dependsOn": ["^check:fix"],
      "cache": false,
      "outputs": [],
      "inputs": ["**/*.py", "**/*.{js,ts,jsx,tsx,json}", "ruff.toml", "pyproject.toml", "biome.json"]
    },
    "type-check": {
      "dependsOn": ["^type-check"],
      "outputs": [],
      "inputs": ["**/*.py", "mypy.ini", "pyproject.toml"]
    },
    "clean": {
      "cache": false,
      "outputs": []
    }
  }
}
